{
  "private": true,
  "scripts": {
    "dev": "nuxt dev",
    "build": "nuxt build",
    "generate": "nuxt generate",
    "preview": "nuxt preview",
    "postinstall": "nuxt prepare",
    "deploy": "node deploy.js",
    "serv": "npm run build && npm run deploy"
  },
  "devDependencies": {
    "@nuxtjs/tailwindcss": "^6.1.3",
    "nuxt": "3.0.0",
    "dotenv": "^10.0.0",
    "ftp-deploy": "^2.4.3",
    "ftp": "latest"
  },
  "dependencies": {
    "mysql2": "^2.3.3"
  }
}
