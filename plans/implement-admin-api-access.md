# Implement Admin API Access in helpers/toAdminApi.js

- [ ] Update `helpers/toAdminApi.js` to load environment variables using `dotenv`.
- [ ] Implement admin token generation logic within `helpers/toAdminApi.js` (or import it).
- [ ] Add `Cookie: token=<token>` header to the `fetch` request in `helpers/toAdminApi.js`.
- [ ] Test the script to verify it successfully accesses `http://localhost:3000/api/admin/test`.
